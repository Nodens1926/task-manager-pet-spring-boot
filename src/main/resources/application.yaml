spring:
  application:
    name: task-manager-pet-spring-boot
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/task_manager_db
    username: postgres
    password: postgres
  jpa:
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: none

job:
  hello: "*/2 * * * * *"
hello:
  name: name

app:
  version: 1.1
  mode: dev
  name: ivan's project